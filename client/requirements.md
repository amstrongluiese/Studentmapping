## Packages
leaflet | Interactive maps
react-leaflet | React bindings for Leaflet
@types/leaflet | TypeScript definitions for Leaflet

## Notes
The map is strictly bounded to the Laguna province coordinates to create a "focused" effect.
Custom DivIcons are used to render the markers with student counts natively embedded in the map.
